---
title: A Mathematical Theory of Communication - 第一章 熵
original_title: A Mathematical Theory of Communication
author: Claude E. Shannon
chapter: 1
status: study-notes
source_repo: https://github.com/JKevinXu/reading-notes-a-mathematical-theory-of-computation
source_note: entropy.md
---

# 第一章：熵（Entropy）

## 一句话结论

熵衡量的是：在已知概率分布的情况下，确定一个随机结果平均还需要多少信息。

熵也可以理解为随机变量的不确定性、选择空间或平均信息量。

## 1. 为什么需要熵

香农希望定义一个量 `H`，用来衡量离散信息源产生的信息量。

给定若干可能事件及其概率：

- 我们知道可能发生什么；
- 但不知道具体哪个事件会发生；
- 需要一个数学量衡量这种不确定性。

## 2. 熵应满足的三个直觉要求

### 2.1 连续性

概率发生小变化时，熵也应该只发生小变化。

例如，公平硬币的概率从 `(0.5, 0.5)` 变成 `(0.51, 0.49)`，熵应该仍然接近 1 bit，而不是突然跳变。

### 2.2 等概率选项越多，熵越大

如果有 `n` 个等概率选项：

```text
H = log₂ n
```

例如：

- 2 个选项：1 bit
- 4 个选项：2 bits
- 8 个选项：3 bits

bit 可以直观理解为一次二选一所提供的信息量单位。

### 2.3 分阶段选择可以分解

复杂选择的信息量，应等于：

```text
第一阶段的信息量
+ 第二阶段信息量的概率加权平均
```

例如，从 `{1, 2, 3, 4}` 中等概率选一个：

1. 先判断它属于 `{1,2}` 还是 `{3,4}`：需要 1 bit；
2. 再在对应的一组中进行二选一：平均还需要 1 bit。

总信息量：

```text
1 + 1 = 2 bits = log₂4
```

## 3. 熵的公式

使用 bit 作为单位时：

```text
H(X) = -Σᵢ pᵢ log₂ pᵢ
```

其中：

- `pᵢ` 是第 `i` 个结果发生的概率；
- `H(X)` 是随机变量 `X` 的熵。

单个事件的信息量可以写成：

```text
I(x) = -log₂ p(x)
```

因此：

- 越不可能发生的事件，发生时携带的信息越多；
- 越常见的事件，发生时携带的信息越少；
- 熵是单个事件信息量的概率加权平均。

## 4. 二元熵

对于两个结果，概率分别为 `p` 和 `1-p`：

```text
H(p) = -p log₂p - (1-p)log₂(1-p)
```

重要特征：

- `p = 0` 或 `p = 1`：结果完全确定，熵为 0；
- `p = 0.5`：不确定性最大，熵为 1 bit；
- 熵关于 `p = 0.5` 对称。

## 5. 熵的基本性质

### 5.1 确定性时熵为零

只有一个结果的概率为 1，其余为 0 时：

```text
H = 0
```

### 5.2 固定结果数量时，均匀分布熵最大

例如，三个结果的概率为：

```text
(1/3, 1/3, 1/3)
```

比：

```text
(1/2, 1/4, 1/4)
```

具有更高的熵，因为前者更难预测。

### 5.3 联合熵

两个随机变量 `X` 和 `Y` 的联合熵为：

```text
H(X,Y)
```

它满足：

```text
H(X,Y) ≤ H(X) + H(Y)
```

当 `X` 和 `Y` 独立时取等号。

如果 `Y` 完全由 `X` 决定，那么：

```text
H(X,Y) = H(X) = H(Y)
```

而不是两者之和，因为知道其中一个以后，另一个不再提供额外不确定性。

### 5.4 条件熵与链式法则

条件熵表示在已经知道 `X` 的情况下，`Y` 还剩多少不确定性：

```text
H(Y|X)
```

链式法则：

```text
H(X,Y) = H(X) + H(Y|X)
```

也就是：

```text
同时确定 X 和 Y 所需的信息
= 先确定 X 所需的信息
+ 知道 X 后确定 Y 所需的信息
```

知识不会增加不确定性：

```text
H(Y|X) ≤ H(Y)
```

- 如果 `X` 与 `Y` 独立，等号成立；
- 如果知道 `X` 能帮助预测 `Y`，条件熵变小；
- 如果 `X` 完全决定 `Y`，则 `H(Y|X)=0`。

## 6. 复习题答案

### 题目

```text
P(A)=1/2
P(B)=1/4
P(C)=1/4
```

### 6.1 X 的熵

```text
H(X)
= -[1/2 log₂(1/2) + 1/4 log₂(1/4) + 1/4 log₂(1/4)]
= 1.5 bits
```

直觉解释：

- 先判断是不是 A：需要 1 bit；
- 如果不是 A（概率 1/2），再用 1 bit 区分 B、C；
- 平均信息量为 `1 + 1/2 × 1 = 1.5 bits`。

### 6.2 已知 X 不可能是 A

剩余概率变成：

```text
P(B)=1/2
P(C)=1/2
```

所以剩余不确定性为：

```text
H = 1 bit
```

### 6.3 哪种分布熵更大

比较：

```text
(1/2, 1/4, 1/4) → 1.5 bits
(1/3, 1/3, 1/3) → log₂3 ≈ 1.585 bits
```

因此均匀分布 `(1/3, 1/3, 1/3)` 的熵更大。

## 7. 本章最重要的六句话

1. 熵衡量不确定性。
2. 罕见事件携带更多信息。
3. 等概率分布的熵最大。
4. 确定性事件的熵为 0。
5. 独立事件的联合熵等于各自熵之和。
6. 已知信息不会增加剩余不确定性。

## 8. 与后续章节的连接

- 熵描述单次随机选择的不确定性；
- 信息源章节会研究连续产生符号的随机过程；
- 离散无噪信道章节会研究单位时间最多能传输多少信息；
- 后续核心问题是：如何利用概率结构进行编码和压缩。

## 参考来源

- Shannon, Claude E. “A Mathematical Theory of Communication.”
- GitHub 原始笔记：[`entropy.md`](https://github.com/JKevinXu/reading-notes-a-mathematical-theory-of-computation/blob/main/entropy.md)
- GitHub 仓库：https://github.com/JKevinXu/reading-notes-a-mathematical-theory-of-computation
